version: "3.8"

services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5050:80" # Map host:container
    container_name: react-app-container
    restart: always
